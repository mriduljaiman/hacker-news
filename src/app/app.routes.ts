import { Routes } from '@angular/router';
import { TopStoriesComponent } from './top-stories/top-stories.component';

export const routes: Routes = [
    {path : 'posts', component:TopStoriesComponent}
];
